package com.builder_design;

public class Builder {
	/*
	 * first of all, builder patter is a type of creational design pattern as it talks about 
	 * creating an object, 2nd thing is it helps in creating a complex object using simple step by step
	 * This design makes use of parameterized constructor
	 * It actually seperates the construction logic from its representation means the same
	 * logic for constructing the object results into different representations based on the input
	 * 
	 */
}
